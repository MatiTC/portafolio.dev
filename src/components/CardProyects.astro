---
import ButtonLink from './ButtonLink.astro';
import GitHub from './icons/GitHub.astro';
import Lock from './icons/Lock.astro';
import LockOpen from './icons/LockOpen.astro';
import WebSite from './icons/WebSite.astro';
const { title, descripcion, link, github, image, alt, estado, tags, color } =
  Astro.props;
---

<div class="flex flex-col w-auto ">
  <img
    class="rounded-tl-g w-max h-48 border-solid border-2"
    src={image}
    alt={alt}
  />
  <div class="flex flex-col justify-start p-4 px-2 h-40">
    <div class=`text-xl font-semibold mb-2  ${color}`>
      {title}
    </div>
    <p class="mt-0 mb-3 font-normal text-pretty dark:text-slate-300 text-slate-700">
      {descripcion}
    </p>
  </div>

  <hr class="my-2 dark:border-gray-200 sm:mx-auto border-gray-700 lg:my-2" />

  <div class="flex flex-col justify-start p-4 px-2">
    <div
      class="flex justify-start my-2 text-lg font-semibold underline dark:decoration-sky-500 decoration-sky-600 dark:text-slate-100 text-slate-800"
    >
      Tecnologias
    </div>
    <div class="grid grid-flow-col auto-cols-max gap-x-3 mb-2">
      {
        tags.map((tag: any) => (
          <div class="relative group">
            <span class="flex gap-x-2">
              <tag.icon
                class={`size-6 flex gap-x-2 rounded-full text-xs dark:bg-[#2a323d] bg-gray-300 ${tag.class}
                dark:hover:bg-slate-900 hover:bg-slate-200 hover:opacity-90 
              `}
              />
            </span>
          </div>
        ))
      }
    </div>
  </div>
  <hr class="my-2 dark:border-gray-200 sm:mx-auto border-gray-700 opacity-80 lg:my-2" />

  <div class="flex flex-col justify-start p-4 px-2">
    <div class="flex justify-start text-900 my-2 text-lg font-semibold underline dark:decoration-pink-500 decoration-pink-700 dark:text-slate-100 text-slate-800">
      Estado del proyecto
    </div>
    <div class="flex flex-row justify-start dark:text-slate-300 text-slate-700 ">
      {estado === 'Privado' ? <Lock /> : <LockOpen />}
      <p class="mt-1 mx-1 font-normal text-pretty">{estado}</p>
    </div>
  </div>

  <div class="p-4 md:p-2 flex justify-around">
    <ButtonLink href={github}>
      <GitHub class="size-8 md:size-6" />
      GitHub
    </ButtonLink>
    <ButtonLink href={link}>
      <WebSite class="size-8 md:size-6" />
      Web
    </ButtonLink>
  </div>
</div>
